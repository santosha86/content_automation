# Machine-writable control surface — the dashboard Config page reads/writes this file.
# (settings.yaml stays hand-edited; nothing in the UI touches it.)
checkpoints:
  story_pick: auto
  hook_pick: auto
  script_approval: auto
  storyboard_approval: auto
  publish: manual
providers:
  scout: openrouter
  writer: openrouter
  reviewer: openrouter
  voice: elevenlabs
  broll: pexels
autopilot:
  enabled: false      # daily hands-off render; turn on from the dashboard when ready (it spends)
  time: "09:00"
